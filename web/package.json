{
  "dependencies": {
    "playwright": "^1.63.0"
  },
  "devDependencies": {
    "wrangler": "^4.131.0"
  },
  "scripts": {
    "build": "bash ../scripts/build_web_dist.sh",
    "predeploy": "npm run build",
    "deploy": "wrangler deploy --config ../wrangler.jsonc",
    "prepreview": "npm run build",
    "preview": "wrangler dev --config ../wrangler.jsonc",
    "test:rig": "node app/test_canonical_rig.mjs",
    "test:single-view": "node app/test_single_view.mjs"
  }
}
