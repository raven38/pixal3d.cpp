Temporary integration note: PR #1 conflict resolution preserves the validated decoder-side `src/pixal3d_wasm.cpp` as the active shared WASM translation unit and preserves the incoming Texture Flow WASM implementation separately at `src/pixal3d_texture_wasm.cpp`. The next partial-E2E integration step should fold `pixal3d_texture_run` and its exports into the shared module without dropping shape/texture decoder exports. This avoids resolving the large shared source by choosing only one side and losing validated functionality.
